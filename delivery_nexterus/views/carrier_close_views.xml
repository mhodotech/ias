<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <!-- Action for Close Carriers -->
  <record id="action_close_carriers" model="ir.actions.act_window">
    <field name="name">Close Carriers</field>
    <field name="res_model">stock.picking</field>
    <field name="view_mode">list,form</field>
    <field name="domain">[
      ('picking_type_id.code', '=', 'outgoing'),
      ('state', '=', 'assigned'),
      ('carrier_id', '!=', False),
    ]</field>
    <field name="context">{
      'search_default_state_ready': 1,
      'search_default_delivery_orders': 1,
      'search_default_group_by_scheduled_date': 1
      'search_default_group_by_carrier': 2,
    }</field>
    <field name="search_view_id" ref="view_picking_close_carrier_search"/>
    <field name="help" type="html">
      <p class="o_view_nocontent_smiling_face">
        No delivery orders ready to close
      </p>
      <p>
        This view shows all delivery orders that are ready (status = Ready)
        and can be closed. Select multiple orders and use the "Close Carriers"
        action to mark them as done and notify configured recipients.
      </p>
    </field>
  </record>

  <!-- Server action for Close Carriers -->
  <record id="action_server_close_carriers" model="ir.actions.server">
    <field name="name">Close Carriers</field>
    <field name="model_id" ref="stock.model_stock_picking"/>
    <field name="binding_model_id" ref="stock.model_stock_picking"/>
    <field name="binding_view_types">list</field>
    <field name="state">code</field>
    <field name="code">
if records:
    # Create wizard with selected pickings
    wizard = env['close.carrier.wizard'].create({
        'picking_ids': [(6, 0, records.ids)]
    })
    # Return action to open wizard
    action = {
        'name': 'Close Carriers',
        'type': 'ir.actions.act_window',
        'res_model': 'close.carrier.wizard',
        'res_id': wizard.id,
        'view_mode': 'form',
        'target': 'new',
    }
    </field>
  </record>

  <!-- Menu for Close Carriers -->
  <menuitem id="menu_close_carriers"
            name="Close Carriers"
            parent="stock.menu_stock_warehouse_mgmt"
            action="action_close_carriers"
            sequence="50"
            groups="stock.group_stock_user"/>
</odoo>